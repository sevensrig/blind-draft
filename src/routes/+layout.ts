// Everything runs in the browser, so the shell is just a static file on Vercel.
// Keeps adapter-auto at zero config and guarantees no server round-trips at play time.
export const prerender = true;
